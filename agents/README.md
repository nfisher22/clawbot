# Agents
Each subfolder is a self-contained agent.
New agent = new folder here, NOT a new repo.

- hatfield-cfo/   → Mr. Soul CFO (Python, generates .docx/.xlsx/.pptx)
- appfolio-kpi/   → AppFolio KPI reporter (in progress)
